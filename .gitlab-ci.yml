image: python:3-alpine

before_script:
  - apk update
  - apk add zip

stages:
  - test
  - build

test:
  stage: test
  script:
    - python -mjson.tool 'pf1e_cn/module.json' > /dev/null

build:
  stage: build
  script:
    - zip pf1e_cn.zip -r pf1e_cn -x ".*"
  artifacts:
    name: pf1e_cn
    when: on_success
    paths:
      - pf1e_cn.zip
  when: on_success
  only:
    - tags
    - master
